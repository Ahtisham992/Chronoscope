{
  "name": "chronoscope",
  "version": "1.0.0",
  "description": "Watch any website live its whole life, in one shot.",
  "scripts": {
    "dev": "concurrently \"npm run dev -w web\" \"npm run dev -w api\" \"npm run worker:render -w api\" \"npm run worker:stitch -w api\"",
    "build": "npm run build --workspaces",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "apps/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.0.0"
  }
}
